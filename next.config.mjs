/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['raw.githubusercontent.com', 'static.wikia.nocookie.net', 'disney.fandom.com'],
    },
};

export default nextConfig;